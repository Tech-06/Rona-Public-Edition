"""Namespace directory for installed custom tool packages.

Each installed package lives in its own subdirectory here
(``toolbox/custom/<package_id>/``) and is fetched, configured and removed by
``toolbox.manager``. Nothing under this directory (other than this file) is
tracked in git -- see the repository's .gitignore.
"""
