fastapi>=0.115
uvicorn[standard]>=0.30
openai>=1.40
pydantic>=2.7
pydantic-settings>=2.3
langgraph>=1.0
langgraph-checkpoint-sqlite>=2.0
aiosqlite>=0.20
beautifulsoup4>=4.12
httpx>=0.27
python-dotenv>=1.0
google-genai>=1.0
apscheduler>=3.10,<4
tzdata>=2024.1; platform_system == "Windows"

# Optional tool packages (installed on demand with `rona tools install <id>`
# from the Rona Tools catalog) bring their own dependencies --
# e.g. tavily-python, deepl, requests, google-api-python-client -- and pip
# -install them automatically at install time. They are not listed here.
